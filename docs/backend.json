
{
  "entities": {
    "Product": {
      "title": "Product",
      "description": "A food item in the menu",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "price": { "type": "number" },
        "description": { "type": "string" },
        "category": { "type": "string" },
        "imageUrl": { "type": "string" },
        "isVeg": { "type": "boolean" },
        "vendorId": { "type": "string" },
        "restaurantName": { "type": "string" },
        "badges": { 
          "type": "array", 
          "items": { 
            "type": "string",
            "enum": ["Bestseller", "Featured", "Trending"]
          } 
        }
      },
      "required": ["name", "price", "vendorId"]
    },
    "Order": {
      "title": "Order",
      "description": "Customer order details and status",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "vendorId": { "type": "string" },
        "deliveryPartnerId": { "type": "string" },
        "items": { "type": "array" },
        "total": { "type": "number" },
        "status": { 
          "type": "string", 
          "enum": ["Placed", "Accepted", "Preparing", "Ready for Pickup", "Picked Up", "Out for Delivery", "Delivered"] 
        },
        "address": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Vendor": {
      "title": "Vendor",
      "description": "Store owner details",
      "type": "object",
      "properties": {
        "storeName": { "type": "string" },
        "description": { "type": "string" },
        "category": { "type": "string" },
        "imageUrl": { "type": "string" },
        "bannerUrl": { "type": "string" },
        "rating": { "type": "number" },
        "deliveryTime": { "type": "string" },
        "address": { "type": "string" },
        "walletBalance": { "type": "number" }
      }
    },
    "DeliveryPartner": {
      "title": "Delivery Partner",
      "description": "Delivery executive details",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "status": { "type": "string", "enum": ["Online", "Offline"] },
        "currentOrderId": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["email"]
  },
  "firestore": {
    "/products/{productId}": { "schema": "#/entities/Product", "description": "Menu items" },
    "/orders/{orderId}": { "schema": "#/entities/Order", "description": "Global orders collection" },
    "/vendors/{vendorId}": { "schema": "#/entities/Vendor", "description": "Store profiles" },
    "/delivery_partners/{partnerId}": { "schema": "#/entities/DeliveryPartner", "description": "Delivery staff" },
    "/users/{userId}/profile": { "schema": "object", "description": "User profile and role data" },
    "/banners/{bannerId}": { "schema": "object", "description": "App-wide promotional banners" },
    "/coupons/{couponId}": { "schema": "object", "description": "Discount codes" }
  }
}
